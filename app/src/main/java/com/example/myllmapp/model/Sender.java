package com.example.myllmapp.model;

/**
 * Enum to represent the sender of a message.
 * 用于表示消息发送者的枚举。
 */
public enum Sender {
    USER, // Represents a message sent by the user / 代表用户发送的消息
    LLM   // Represents a message sent by the Large Language Model / 代表大模型发送的消息
}
