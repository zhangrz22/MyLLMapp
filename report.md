# 大模型对话 APP - 项目设计文档

## 1. 需求分析

### 1.1 系统角色

* **普通用户:** 使用 APP 与大语言模型进行交互，查看和管理对话历史。

### 1.2 功能概述

* **对话系统:** 基于大语言模型的实时多轮对话，支持流式输出。

* **对话历史管理:** 对话自动保存、查看、继续与删除功能。

* **模型与参数设置:** 多种大语言模型选择与相关参数配置。

* **联网能力:** 对于支持的模型，提供联网搜索最新信息的能力。

* **UI主题:** 支持日间/夜间模式切换和自动保存偏好设置。

## 2. 项目主要界面的设计

项目包含三个主要界面：

* **主界面 (`MainActivity`)**:
    * 顶部显示标题 "对话历史"。
    * 顶部工具栏包含夜间模式切换按钮。
    * 主体区域使用 `RecyclerView` 列表展示过往的对话记录（显示对话开始时间）。
    * 如果列表为空，显示 "暂无对话历史" 的提示文本。
    * 右下角有一个悬浮操作按钮 (FAB)，用于创建新对话。
    * 每个历史记录项包含删除按钮。

* **聊天界面 (`ChatActivity`)**:
    * 顶部显示标题 "对话"。
    * 顶部工具栏包含返回按钮和设置按钮。
    * 主体区域使用 `RecyclerView` 显示聊天消息气泡，区分用户（靠右）和 LLM（靠左）。
    * 底部包含一个多行文本输入框 (`EditText`) 和一个发送按钮 (`ImageButton`)。
    * 消息列表会自动滚动到最新消息。
    * 支持流式输出时实时显示 LLM 回复。

* **设置界面 (`SettingsActivity`)**:
    * 顶部显示标题 "设置"。
    * 模型设置区域，用户可选择不同的 AI 模型。
    * 流式输出开关，控制是否使用流式输出功能。
    * 联网搜索开关，控制是否允许 AI 联网查询信息。

## 3. 项目关键的 Activity 和实现方式

* **主界面 (`MainActivity`):**
    * **职责:** 显示所有过去的对话列表，让用户能开始新对话或打开旧对话，管理夜间模式。
    * **实现方式:**
        * 使用 View Binding 技术方便地控制屏幕上的按钮、列表等元素。
        * 从数据库获取所有保存过的对话记录。
        * 使用 LiveData 技术，当数据库中的对话记录发生变化时，能自动收到通知。
        * 设置屏幕上的列表 (RecyclerView) 和它的适配器 (Adapter)，显示对话数据。
        * 当收到数据库变化的通知时，就更新屏幕上的列表显示。
        * 处理用户点击列表项的操作：当用户点击某条旧对话时，打开聊天屏幕并加载指定对话。
        * 处理用户点击"新建对话"按钮的操作：打开聊天屏幕，并标记为新对话。
        * 夜间模式功能：通过工具栏上的图标按钮实现日间/夜间模式的切换。
        * 使用 SharedPreferences 保存用户的夜间模式偏好设置。

* **聊天界面 (`ChatActivity`):**
    * **职责:** 显示某一个具体对话的所有消息，处理用户的文字输入，发送消息给模型，保存新消息，并管理当前是在新对话还是旧对话中。
    * **实现方式:**
        * 使用 View Binding 技术控制屏幕上的消息列表、输入框、发送按钮等。
        * 从主界面接收信息，判断当前是加载旧对话还是开始新对话。
        * 设置消息列表和适配器，显示聊天消息，并默认滚动到底部。
        * 如果是加载旧对话，从数据库读取对话的所有消息，并利用 LiveData 技术自动更新到屏幕。
        * 处理用户点击"发送"按钮的操作：
            * 获取用户在输入框里输入的文字。
            * 检查是否是新对话: 如果是第一条消息，先在数据库里创建一个新的对话记录。
            * 把用户输入的消息保存到数据库。
            * 调用 API 获取 AI 回复，并根据设置决定是否使用流式输出。
        * 处理用户点击"设置"按钮的操作：打开设置界面，并传递当前对话 ID。

* **设置界面 (`SettingsActivity`):**
    * **职责:** 允许用户配置应用的各种设置，如选择 AI 模型、开启/关闭流式输出和联网搜索。
    * **实现方式:**
        * 使用 View Binding 技术控制各种设置控件。
        * 从 ChatActivity 接收当前对话的 ID（如果有）。
        * 使用 SharedPreferences 保存和加载用户的设置选项。
        * 设置模型选择下拉框，显示可用的 AI 模型选项。
        * 设置流式输出和联网搜索的开关。
        * 处理设置变更，保存到 SharedPreferences 并更新当前对话的相关设置。
        * 处理某些设置间的联动关系，如特定模型不支持联网搜索时自动禁用该选项。

## 4. 功能详细说明

### 4.1 对话系统

* **多轮对话:** 
    * 支持用户与AI进行连续的多轮对话，每轮对话都会保留上下文。
    * AI响应会考虑之前的对话内容，实现连贯的交流体验。

* **流式输出:** 
    * 用户可以实时看到AI生成回复的过程，逐字显示。
    * 增强交互体验，减少等待感，提供类似与真人对话的即时反馈。
    * 可在设置中开启或关闭此功能。

* **内容保存:** 
    * 所有对话内容自动保存至本地数据库。
    * 支持断点续传，用户可随时离开并在之后继续之前的对话。

### 4.2 对话历史管理

* **历史列表:** 
    * 在主界面清晰展示所有历史对话，按时间倒序排列。
    * 每个历史项显示对话的开始时间和首条消息内容作为预览。

* **继续对话:** 
    * 点击任意历史对话项可快速进入对话界面，并加载全部历史消息。
    * 用户可直接在历史对话基础上继续与AI交流。

* **删除功能:** 
    * 提供便捷的删除按钮，用户可移除不需要的历史对话。
    * 删除前会弹出确认对话框，防止误操作。
    * 删除操作会清除该对话的所有相关消息数据。

### 4.3 模型与参数设置

* **多模型支持:** 
    * 应用集成了多种大语言模型，包括"通义千问-plus"和"deepseek-v3"。
    * 每个模型有其特有的能力和特点，用户可根据需要选择。

* **流式输出配置:** 
    * 用户可以根据个人偏好和网络条件，选择启用或禁用流式输出功能。
    * 启用流式输出时，AI响应将实时逐字显示。
    * 禁用时，等待完整响应后一次性显示，适合网络条件不佳的情况。

* **模型关联设置:** 
    * 应用会根据所选模型自动调整相关设置选项的可用性。
    * 例如，某些模型不支持联网搜索功能，此时相关选项会自动禁用。

### 4.4 联网搜索能力

* **信息获取:** 
    * 对于支持的模型，可开启联网搜索功能，使AI能获取最新网络信息。
    * 这大大增强了AI回答时事问题、获取最新数据的能力。

* **配置控制:** 
    * 用户可通过设置页面的开关来控制是否允许AI联网搜索。
    * 此设置可针对每个对话单独配置，提供灵活性。

* **模型限制:** 
    * 界面会智能识别当前模型是否支持联网搜索功能。
    * 如模型不支持，相关设置将自动禁用，并给予用户明确提示。

### 4.5 UI主题系统

* **日夜间模式:** 
    * 支持日间（明亮）和夜间（深色）两种界面主题。
    * 夜间模式使用深色背景和适配的文字颜色，减少夜间使用时的眼部疲劳。

* **手动切换:** 
    * 在主界面顶部提供了直观的切换按钮（太阳/月亮图标）。
    * 日间模式下显示太阳图标，夜间模式下显示月亮图标。
    * 切换时会显示相应的提示信息，并平滑过渡到新主题。

* **设置保存:** 
    * 用户的主题选择会自动保存在应用偏好设置中。
    * 应用重启后会自动应用上次选择的主题，无需重新设置。

* **系统集成:** 
    * 通过Android的主题系统实现，确保所有UI元素在不同主题下都有良好的显示效果。
    * 所有颜色资源都为日夜间模式分别定义，保证视觉一致性。


## 5. 项目挑战与解决方案

### 5.1 已解决问题

* **Gradle 配置:** 解决了依赖库版本冲突和配置错误问题，确保所有依赖库正确集成。

* **资源管理:** 确保所有界面元素使用正确的资源引用，避免资源未找到错误。

* **View Binding 配置:** 成功启用并使用 View Binding 简化视图访问。

* **夜间模式切换:** 实现了平滑的主题切换和状态保存功能。

* **模型设置关联:** 处理了不同模型支持的功能差异，确保用户界面正确反映可用选项。

* **API 对接复杂性:** 成功处理异步网络请求、构造复杂请求体、解析不同结构的 API 响应。

* **API Key 安全管理:** 实现了安全存储和使用 API Key 的机制，避免敏感信息泄露。

* **上下文管理:** 开发了有效的上下文传递系统，确保 AI 能够理解对话历史而不超出 token 限制。

* **UI/UX 优化:** 实现了流畅的加载状态提示、良好的长列表滚动性能和美观的界面设计。

### 5.2 技术亮点

* **多模型支持:** 应用支持多种大语言模型，并为每种模型提供适当的配置选项。

* **流式输出:** 实现了 AI 回复的实时流式显示，提升用户体验。

* **响应式设计:** 使用 LiveData 和观察者模式，实现 UI 与数据变化的自动同步。

* **主题适配:** 完善的日间/夜间模式支持，所有 UI 元素在不同主题下都有良好的显示效果。

* **多线程处理:** 使用专用执行器进行 I/O 操作，确保主线程流畅运行。

* **数据持久化:** 完整的数据库设计，支持对话和消息的有效存储和管理。

## 6. 结论

大模型对话 APP 已实现了全面的对话功能、历史管理、设置系统和夜间模式支持，为用户提供了优质的大模型交互体验。通过简洁直观的界面设计和稳健的技术实现，应用成功满足了用户与大语言模型进行对话的各种需求。项目采用了现代 Android 开发技术和架构模式，确保了代码的可维护性和可扩展性，为未来的功能拓展和性能优化奠定了坚实基础。
